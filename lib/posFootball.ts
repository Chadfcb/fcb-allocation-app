// Shared constant for POS > Football POS — a flat file library, unlike
// Labels' brand/size split (see lib/posLabels.ts). File-display helpers
// (icons, image detection, byte formatting, safe storage names) are
// generic and already live in lib/events.ts; reused from there.

export const POS_FOOTBALL_FILES_BUCKET = "pos-football-files";
